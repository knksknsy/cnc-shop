/*import { TestBed, inject } from '@angular/core/testing';

import { OrderHistoryResolverService } from './order-history-resolver.service';

describe('OrderHistoryResolverService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [OrderHistoryResolverService]
    });
  });

  it('should be created', inject([OrderHistoryResolverService], (service: OrderHistoryResolverService) => {
    expect(service).toBeTruthy();
  }));
});
*/