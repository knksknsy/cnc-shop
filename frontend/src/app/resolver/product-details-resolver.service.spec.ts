/*import { TestBed, inject } from '@angular/core/testing';

import { ProductDetailsResolverService } from './product-details-resolver.service';

describe('ProductDetailsResolverService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ProductDetailsResolverService]
    });
  });

  it('should be created', inject([ProductDetailsResolverService], (service: ProductDetailsResolverService) => {
    expect(service).toBeTruthy();
  }));
});
*/