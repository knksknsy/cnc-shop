/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StaticBilderComponent } from './static-bilder.component';

describe('StaticBilderComponent', () => {
  let component: StaticBilderComponent;
  let fixture: ComponentFixture<StaticBilderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StaticBilderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StaticBilderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/